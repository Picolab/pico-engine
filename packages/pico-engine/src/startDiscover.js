module.exports = function startDiscover(pe){

    // TODO

    /*
    pe.signalEvent({
        eci: TODO,
        domain: "discover",
        type: "engine_found",
        attrs: {}
    }, function(err, response){
    });
    */
};
